- unless items.nil?
  - items.each do |t|
    %li{:id => "#{t.id}"}
      =t.title
      =t.due_date
      %span{:class => "status", :value => "#{t.status}"} status indicator 
      %input{:class => "due_date"} 
      
